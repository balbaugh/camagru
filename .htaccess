# Disable Directory Indexing
Options -Indexes

# Deny Access to the .htaccess File
<Files .htaccess>
order allow,deny
deny from all
</Files>

# max file size for uploads in bytes (default 2MB)
php_value upload_max_filesize 10M
